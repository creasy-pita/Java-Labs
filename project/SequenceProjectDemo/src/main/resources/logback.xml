<configuration>
	<property name="FILE_LOG_PATTERN"
		value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [%X{traceId:-}] %X{user.username} %X{req.requestURI} %X{req.queryString} %logger{36}:%line - %msg%n" />
	<property name="CONSOLE_LOG_PATTERN"
		value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [%X{traceId:-}] %X{user.username} %X{req.requestURI} %logger{36}:%line - %msg%n" />
	<!-- 设置一个路径变量，表示当前工作目录 -->
	<property name="LOG_HOME" value="." />
	<property name="PROJECT_NAME" value="transfer-app" />
	<property name="LOG_PATH" value="${LOG_HOME}/logs/${PROJECT_NAME}/" />
	<appender name="stdout" class="ch.qos.logback.core.ConsoleAppender">
		<encoder>
			<pattern>${CONSOLE_LOG_PATTERN}</pattern>
		</encoder>
	</appender>

	<appender name="rollingFile"
		class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>${LOG_PATH}/${PROJECT_NAME}.log</file>
		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<fileNamePattern>${LOG_PATH}/${PROJECT_NAME}.%d{yyyyMMdd}.log.%i</fileNamePattern>
			<maxHistory>20</maxHistory>
			<timeBasedFileNamingAndTriggeringPolicy
				class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
				<maxFileSize>10MB</maxFileSize>
			</timeBasedFileNamingAndTriggeringPolicy>
		</rollingPolicy>
		<encoder>
			<pattern>${FILE_LOG_PATTERN}</pattern>
		</encoder>
	</appender>
	<appender name="ASYNC" class="ch.qos.logback.classic.AsyncAppender">
		<discardingThreshold>0</discardingThreshold>
		<queueSize>512</queueSize>
		<appender-ref ref="rollingFile" />
	</appender>

	<logger name="com.gisquest" level="debug"/>
	<logger name="com.gisquest.platform.service.TransferService" level="debug"/>
	<root level="info">
<!--		<appender-ref ref="ASYNC" />-->
		<appender-ref ref="rollingFile" />
		<appender-ref ref="stdout" />
	</root>

</configuration>
